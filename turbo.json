{
  "remoteCache":{
    "enabled": true,
    "timeout": 10,
    "uploadTimeout": 10
  },
  "$schema": "https://turbo.build/schema.json",
  "ui": "stream",
  "tasks": {
    "hello": {
      "dependsOn": ["^hello"],
      "outputLogs": "new-only",
      "outputs": ["dist/**"]
    }
  }
}
